# Domashniy Bar Handover

Last updated: 2026-05-01

## Project

Domashniy Bar is a phone-oriented Expo / React Native Web / TypeScript app for picking cocktails from ingredients already available at home.

Local path:

```text
C:\Users\Sergej\Documents\Codex\domashniy-bar
```

## Current State

- GitHub remote: `https://github.com/sergkreis/Home-bar.git`.
- The GitHub CLI (`gh`) is not available in PATH on this machine.
- The Codex GitHub connector is reachable, but currently returns no accessible repositories.
- Main app UI lives in `App.tsx`.
- Reusable UI pieces live in `src/components/`.
- Cocktail matching and shopping advice live in `src/utils/`.
- Cocktail/ingredient data lives in `src/data/` and is generated by `scripts/import-cocktails.mjs`.
- VPS deployment preparation lives in `deploy/`.
- VPS `212.227.28.224` was cleaned on 2026-05-01 and now serves a temporary Home Bar placeholder from `/var/www/home-bar`.
- Old KIKU files and services were backed up to `/root/home-bar-cleanup-backups/20260501-131803` before removal.

## Stack

```text
Expo SDK 54
React 19
React Native 0.81
TypeScript strict mode
npm / package-lock.json
Static web export served by nginx
```

## Implemented Features

- Home screen with summary counters.
- Ingredient picker grouped by category.
- Ingredient search inside the picker.
- "What to drink tonight" quick modes.
- Taste filters: refreshing, sweet, sour, strong, bitter.
- Ranked cocktail list by ingredient match.
- Expanded cocktail recipe view with glass, ingredients, steps, and garnish.
- Shopping suggestions for 1-2 missing ingredients.
- Generated cocktail database from TheCocktailDB plus curated cocktail names.

## Data Snapshot

As of 2026-05-01:

```text
Cocktails: 72
Ingredients: 93
Recipe ingredient links: 279
Starter ingredients: 18
```

Ingredient categories:

```text
spirit: 20
liqueur: 22
mixer: 18
other: 19
citrus: 7
sweetener: 7
```

## Current Unfinished Work

The "save home bar" step was started but should still be treated as pending verification:

- `@react-native-async-storage/async-storage` was installed.
- `App.tsx` now loads/saves `selectedIngredients` under the storage key `domashniy-bar:selected-ingredients`.
- `npx tsc --noEmit` passed after this change.
- Runtime verification in Expo Web was interrupted before completion.

Before continuing feature work, verify this on web and/or Expo Go.

## Known Issues From Review

- `CocktailResults` was updated to reset the expanded card when the result list changes.
- `scripts/import-cocktails.mjs` was fixed so `gin` no longer matches inside `ginger`, and `Carbonated water` is normalized to `soda-water`.
- `App.tsx` is still doing too much: state, persistence, screen switching, filtering, shopping suggestions, and layout all live in one file.
- Generated data still contains a mix of translated Russian names and raw English ingredient names.

## Useful Commands

Install dependencies:

```bash
npm install
```

Run Expo:

```bash
npm start
```

Run web:

```bash
npm run web
```

Build static web:

```bash
npm run build:web
```

Type-check:

```bash
npx tsc --noEmit
```

Regenerate cocktail data:

```bash
npm run import:cocktails
```

## Recommended Next Steps

1. Verify and finish persistent home bar storage.
2. Finish the phone-oriented web/PWA pass.
3. Split `App.tsx` into screens/hooks once the MVP behavior is stable.
4. Deploy static web build to `kreisphoto.de` after explicit approval.
5. Add a small testable layer around matching/shopping logic before expanding the database further.
